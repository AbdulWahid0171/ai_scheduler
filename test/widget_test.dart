import 'package:flutter_test/flutter_test.dart';

import 'package:ai_scheduler/main.dart';

void main() {
  test('SchedulerApp can be constructed', () {
    expect(const SchedulerApp(), isNotNull);
  });
}
